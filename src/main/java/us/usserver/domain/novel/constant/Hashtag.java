package us.usserver.domain.novel.constant;

/**
 * # Hashtag 를 사용해 소설을 소개 한다.
 * 현재 기획은 정해진 Hashtag 약 30개 중에 소설에 맞는 것을 선택 하는 것 이지만, 추후에 변경될 수 있다.
 * 갯수와 종류가 정해져 있기 때문에 Enum 을 사용 하지만 추후에 다른 Package 로 분리될 수 있다.
 */
public enum Hashtag {
    MONCHKIN,
    HASHTAG1,
    HASHTAG2
}
